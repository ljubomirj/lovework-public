[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    llm: marks tests that require a live LLM API key
    slow: marks slow tests (>1 second)
addopts =
    -v
    --tb=short
    -ra
